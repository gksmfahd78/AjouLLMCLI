# Screenshots

Add CLI/TUI screenshots or GIFs here.

Recommended files:

- `tui-session.png`
- `code-context.png`
- `agent-plan.gif`

Suggested capture list:

- Fullscreen TUI session
- `code context` output for a real project
- `agent --plan` or `agent --dry-run` flow
